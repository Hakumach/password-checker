# Password Security Tool

CLI tool that analyzes password strength, flags weak patterns, and generates secure passwords.

## Features
- Strength scoring (length, case, digits, symbols)
- Detects common passwords and weak patterns (sequences, repeats)
- Actionable feedback to improve passwords
- Secure password generator

## Usage
```bash
python main.py
